import SettingSidebar from "@/components/setting/SettingSidebar";

type Props = {};

const page = (props: Props) => {
    return <SettingSidebar className="md:hidden"></SettingSidebar>;
};

export default page;
